 var integer acc;

  integer odd(integer n) {
    print(47);
    return(3*n+1);
  }

  integer even(integer n) {
    print(92);
    return(n/2);
  }

  main {
    acc := arg;
    while (acc != 1) {
      if (acc%2 == 0) {
        acc := even(acc);
      } else {
        acc := odd(acc);
      }
    } 
  }

var boolean continuer := true;
var integer i:= 0, j;

main {

  while (continuer) {
    continuer := false;
    j := 0;
    while (j < arg+1) {
      if (i*i + j*j < arg*arg) {
        print(46);
        continuer := true
      } else {
        print(35)
      };
      print(32);
      j := j+1
    };
    print(10);
    i := i+1
  }
}
